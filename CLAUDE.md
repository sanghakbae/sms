# 영업관리시스템

무하유 표절검사 사업 기준의 B2B 영업 관리. React 18 + Vite 6 + Firebase(Firestore + Google OAuth).
GitHub Actions 로 GitHub Pages 에 배포.

```bash
npm run dev     # 개발 서버 (5173)
npm test        # node --test, src/lib/stats.js 검증
npm run build   # dist + 404.html(SPA 폴백)
```

## 지식 보관함 (Obsidian)

`/Users/mac/Documents/Obsidian Vault` — `obsidian-vault` MCP 로 읽고 쓸 수 있다.

**코드는 저장소에, 왜 그렇게 했는지는 보관함에.**

| 폴더 | 무엇을 넣나 |
|---|---|
| `01 프로젝트/영업관리시스템.md` | 현재 상태·구조·규칙·미해결 과제 |
| `02 결정기록/` | `YYYY-MM-DD 무엇을 정했나.md` — 갈림길에서 왜 이걸 골랐나 |
| `03 데일리/` | `YYYY-MM-DD.md` — 그날 한 일과 막힌 지점 |
| `99 템플릿/` | 새 노트 뼈대. **새 노트는 반드시 템플릿을 따른다** |

### 작업 시작할 때

`01 프로젝트/영업관리시스템.md` 를 읽는다. 거기 '규칙'과 '미해결' 이 이 파일보다 최신이다.
그 문서가 결정기록을 `[[위키링크]]` 로 걸어두므로, 관련 결정이 링크돼 있으면 그것도 읽는다.

### 작업 끝낼 때

아래에 해당하면 **먼저 하겠다고 말하고** 보관함을 갱신한다. 묻지 않고 쓰지는 않는다.

- **선택지가 여럿인 갈림길에서 하나를 골랐다** → `02 결정기록` 에 새 노트.
  템플릿의 '선택지' 표와 '되돌린다면' 을 반드시 채운다. 그게 이 기록의 값어치다.
- **규칙이 생기거나 바뀌었다** (어기면 깨지는 것) → 프로젝트 노트의 '규칙'.
- **과제가 생기거나 닫혔다** → 프로젝트 노트의 '미해결' 체크박스.
- 결정기록을 새로 만들었으면 프로젝트 노트 맨 아래 '결정기록' 목록에도 링크를 추가한다.

### 보관함에 적지 않는 것

코드에서 읽어낼 수 있는 것 — 파일 구조, 함수 시그니처, 스키마 필드 목록, 이미 고친 버그.
그런 건 저장소 `docs/` 에 둔다 (예: `docs/스키마.md`).
적을 값어치가 있는 건 **선택지가 여럿이었을 때 왜 이걸 골랐는가** 뿐이다.

날짜는 상대표현("어제", "지난주") 대신 `YYYY-MM-DD` 로 적는다.

## 반드시 지킬 것

> [!warning] 권한은 두 곳을 같이 고친다
> `src/lib/accounts.js` 와 `firestore.rules` 는 같은 기준을 중복 구현한다.
> 한쪽만 고치면 화면은 열리는데 데이터가 거부되거나 그 반대가 된다.
> `BOOTSTRAP_ADMINS` ↔ `bootstrapAdmins()`, `ALLOWED_DOMAINS` ↔ `isMember()`.
>
> 규칙은 파일만 고쳐선 적용되지 않는다 — 배포해야 한다.
> ```bash
> firebase deploy --only firestore:rules --project sales-management-system-b045a
> ```

- **날짜 비교는 `YYYY-MM-DD` 문자열끼리 한다.** `Date` 로 바꾸면 UTC 로 해석돼
  한국시간 자정~오전 9시에 하루가 어긋난다.
- **집계 로직은 `src/lib/stats.js` 에 순수함수로.** 컴포넌트에 넣지 않는다. 테스트가 여기 붙어 있다.
- **파이프라인 단계·등급·활동종류는 `src/lib/pipeline.js` 한 곳에서 정의한다.** 문자열을 흩뿌리지 않는다.
- **실패는 단계가 아니라 `lost` 플래그다.** `stage` 를 그대로 두므로 어디서 깨졌는지가 남는다.
- **리드(`lead`)는 예상매출에 넣지 않는다.** `preQualified: true` — 아직 영업기회가 아니다.
- 배포 빌드는 `.env` 가 아니라 **저장소 Actions Variables** 에서 값을 받는다.
  로컬만 고치면 배포본이 "설정이 필요합니다" 로 뜬다.

## 코드 스타일

기존 코드에 맞춘다 — 세미콜론 없음, 작은따옴표, 2칸 들여쓰기.
주석은 **한국어로, 왜 그렇게 했는지**를 쓴다. 무엇을 하는지는 코드가 말한다.
